#include <bits/stdc++.h>

using namespace std;

#pragma once

char *generateRandomEquation();

char* toPostfix(char*infix);

void pushStack(int n);
int popStack();
void pushStack(char ch);
char popSack();



void temp();
